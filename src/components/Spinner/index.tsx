import BSSpinner from 'react-bootstrap/Spinner';

const Spinner = () => (
	<BSSpinner
		variant="primary"
		animation="grow"
		role="status"
		className="d-block mx-auto mt-5"
	/>
);

export default Spinner;
